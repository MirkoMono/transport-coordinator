[project]
name = "transport-coordinator"
version = "0.1.0"
description = "Film production transport coordinator"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["apps/api", "packages/solver"]

[tool.ruff]
line-length = 100
target-version = "py312"
