# transport-solver

Capacitated VRP solver for film production transport coordination.

Uses Google OR-Tools (Apache 2.0). Phase 0 ships a placeholder round-robin assigner; full optimization lands in Phase 1.

```bash
pip install -e ".[dev]"
pytest
```
